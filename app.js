$(document).ready(function(){
    //Drop Down Menu
    $(".dropdown-trigger").dropdown();
    //side nav
    $('.sidenav').sidenav();
    // fixed Action btn
    $('.fixed-action-btn').floatingActionButton();
})